import { CustomOverDirective } from './custom-over.directive';

describe('CustomOverDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomOverDirective();
    expect(directive).toBeTruthy();
  });
});
